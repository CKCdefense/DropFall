# DropFall AI 활용 기술 문서

- 게임명: DropFall
- 팀명: DropFall
- 제출자: DropFall 팀(김동현·최두용·추보문)
- 작성일: 2026-08-10


## 1. AI 활용 개요

DropFall은 AI 동료 함께, 몰려오는 몬스터로부터 코어를 방어하는 멀티 실시간 디펜스 게임 입니다.

본 프로젝트에서는 세 가지 방식으로 AI를 활용했습니다.

1. **개발 과정의 AI 활용**
   - Claude Code를 활용하여 게임 기획, 코드 구현, 리팩터링, 테스트 코드 작성 및 오류 분석을 보조했습니다.

2. **에셋 제작 과정의 AI 활용**
   - 배경·로비 이미지 등 한 장짜리 큰 그림은 이미지 생성 AI(Nano Banana)로 초안을 만든 뒤 Aseprite에서 직접 다듬었습니다.
   - 타일셋·이펙트처럼 **규칙으로 표현되는** 그림은 AI에게 Aseprite Lua 생성 스크립트를 작성하게 하고, Aseprite CLI로 실행해 스프라이트를 만들었습니다.

3. **게임 내 AI 활용**
   - AI 동료(티모시)는 전장 내 자원 탐색·채집 자체는 규칙 기반(rule-based) 상태머신으로 자동 수행합니다.
   - 플레이어가 채팅에서 `@티모시`로 말을 걸면, 그 순간에만 LLM이 Tool Calling으로 실제 게임 상태(창고 자원량, 웨이브 상태, 티모시 자신의 상태)를 조회한 뒤 캐릭터로서 자연어로 응답합니다.

AI는 개발 및 게임 내 행동을 보조하는 역할로 활용했으며, 게임 규칙과 최종 구현 방향은 팀이 직접 검토하고 결정했습니다.

## 2. 개발 과정에서의 AI 도구 활용

### 2.1 활용 도구

본 프로젝트에서는 **Claude Code**를 주요 AI 개발 보조 도구로 활용했습니다.

| 활용 도구 | 활용 영역 | 주요 활용 내용 |
|---|---|---|
| Claude Code | 기획 | 기능 스펙 문서 초안 작성 보조 (예: `docs/superpowers/specs/2026-08-07-ai-companion-timothy-design.md`) |
| Claude Code | 구현 | Colyseus 서버 / Phaser 클라이언트 / 공유 시뮬레이션(`packages/shared/src/sim`) 로직 구현 보조 |
| Claude Code | AI 기능 개발 | Anthropic Messages API 연동(`corePersonaClient.ts`), Tool Calling 도구 3종 구현(`companionTools.ts`) 보조 |
| Claude Code | 테스트 및 디버깅 | Vitest 유닛 테스트 작성(fetch mock 기반 26개 케이스 등), 회귀 테스트, 버그 재현 후 수정 |
| Claude Code | 리팩터링 | 채팅 쿨다운 큐잉 로직 개선 등 중복/누락 로직 정리 |
| Claude Code | 문서화 | `docs/backend/`, `docs/frontend/`의 작업 보고서(기획-과정-결과) 수십 건, 세션 인계 문서(`docs/backend/claudesession.md`) 작성 |


### 2.2 AI 활용 개발 절차

프로젝트는 AI에게 전체 게임을 일괄 생성하도록 요청하는 방식이 아니라,
기능 요구사항을 분리하고 각 기능을 검증하며 구현하는 방식으로 진행했습니다.

1. 팀이 구현할 기능과 게임 규칙을 정의했습니다.
2. Claude Code에 기존 프로젝트 구조, 구현 범위 및 제약 조건을 전달했습니다.
3. AI가 구현 또는 수정 방향을 제안하고, 코드 작성을 보조했습니다.
4. 팀이 생성된 코드를 검토하여 프로젝트 구조 및 게임 요구사항과의 일치 여부를 확인했습니다.
5. 신규·수정 기능에 대한 테스트 로직을 작성하고 실행했습니다.
6. 기존 기능에 영향이 없는지 회귀 테스트와 실제 플레이 테스트를 수행했습니다.
7. 검증된 기능만 프로젝트에 반영했습니다.

### 2.3 작업 방식에서 명시적으로 정한 규칙

AI를 "코드를 대신 쓰는 도구"가 아니라 **팀의 작업 규칙을 지키는 협업자**로 다루기 위해, 프로젝트 문서(`docs/backend/claudesession.md`, `팀원룰.md`)에 규칙을 문서화하고 매 세션 전달했습니다. 아래 규칙들은 실제 작업 중 겪은 문제에서 나온 것입니다.

| 규칙 | 정한 이유 |
|---|---|
| **커밋·푸시는 명시적으로 요청받았을 때만 수행** | AI가 임의로 커밋하면 팀원이 편집 중인 파일이 함께 올라가 작업이 섞입니다. 실제로 팀원이 편집 중이던 파일과 얽혀, 스테이징 단계에서 팀원 변경분을 분리해 커밋한 사례가 있었습니다. |
| **작업 단위마다 `docs/`에 기획-과정-결과 보고서 작성 + `docs/README.md` 인덱스 갱신** | "왜 이렇게 짰는가"가 남지 않으면 다음 세션의 AI가 같은 실수를 반복합니다. 실제로 보간 지연을 `PATCH_RATE`에 묶어 두었던 과거 결정이 문서에 남아 있어, 이후 렉 원인 분석에서 곧바로 추적할 수 있었습니다. |
| **숫자는 한 곳에서만 정의(단일 진실 공급원)** | 같은 값이 서버와 클라이언트에 각각 적히면 조용히 어긋납니다. 밸런스 수치는 `packages/shared/src/data/*.json`, 규격 상수는 `constants.ts`로 모으고 zod로 런타임 검증합니다. |
| **추측하지 말고 측정한 뒤 판단** | 원인 추정만으로 고치면 증상만 옮겨 다닙니다. 렉 원인 분석 시 `nslookup`·`curl` 실측으로 경로와 지터를 먼저 재고 나서 코드를 수정했습니다. |
| **검증 없이 "완료"라고 보고하지 않기** | 테스트가 실패했거나 확인하지 못한 항목이 있으면 그대로 보고하도록 지시했습니다. |
| **작업 지시와 결과 보고는 한국어로 통일** | 팀원 전원이 문서를 그대로 읽고 검토할 수 있어야 합니다. |

### 2.4 AI가 사용한 개발 도구

코드 작성 외에, 실제 동작 확인과 에셋 생성을 위해 아래 도구를 AI가 직접 실행하도록 했습니다.

| 도구 | 용도 |
|---|---|
| Vitest | 유닛·회귀 테스트 실행 (`pnpm test`) |
| TypeScript / ESLint | 타입 검사 및 정적 분석 (`pnpm typecheck`, `pnpm lint`) |
| Headless Chrome + Chrome DevTools Protocol | 실제 화면을 띄워 스크린샷으로 시각 검증 (스프라이트 정렬, UI 레이아웃 등) |
| Aseprite CLI | Lua 생성 스크립트 실행 및 아틀라스 빌드 (`pnpm build:atlas`) |
| Git / GitHub Actions | 주제별 커밋 분리, GitHub Pages·홈서버 자동 배포 |


### 사례 1. 기능 구현 요청

#### 요청 목적
채팅에서 AI 동료를 부를 때마다 "@티모시"를 전부 입력해야 하는 불편함을 줄이기 위한 멘션 자동완성 기능

#### 프롬프트
```text
[실제 대화 프롬프트 원문은 로컬 세션 기록에만 남아 있어 이 문서에는 없음 — 담당 팀원이 직접 채워 넣을 것]
```

#### 활용 결과
- AI가 보조한 구현: 채팅 입력창에서 `@`로 시작한 상태에서 Tab을 누르면 `@티모시 `로 자동 완성(`packages/client/src/game/ui/ChatBox.ts`의 `completeMention()`), 입력창 옆에 "@+Tab: 티모시" 힌트 상시 노출
- 팀이 직접 검토·수정한 내용: 멘션 대상이 티모시 하나뿐이라 후보 목록 없이 즉시 완성되도록 범위를 제한
- 검증 방법: Playwright로 `@+Tab → "@티모시 " 자동완성 → 이어 입력 → 전송`까지 수동 시나리오 확인, 콘솔 에러 없음 확인 (커밋 `70df9cb`)

### 사례 2. 테스트 및 회귀 검증 요청

#### 요청 목적
그동안 실제 hchat을 호출하는 유료 통합 테스트(`test:integration`)로만 검증되던 LLM 연동 프로토콜(요청 헤더/바디, 도구 왕복, 에러 처리, 무한루프 방지)에 빠르고 결정론적인 fetch mock 기반 유닛 테스트를 추가

#### 프롬프트
```text
[실제 대화 프롬프트 원문은 로컬 세션 기록에만 남아 있어 이 문서에는 없음 — 담당 팀원이 직접 채워 넣을 것]
```

#### 확인 항목
- provider 자동 선택(direct/hchat 키 우선순위) 정상 동작
- 성공/실패/빈 응답/네트워크 오류 시 처리, direct·hchat 헤더 차이 처리
- 도구 없이 즉답하는 경우와, 도구 호출 → 결과 반영 → 최종 답변까지 왕복하는 경우 모두 정상 동작
- 도구 실행 중 예외가 나도 루프가 죽지 않고, 무한 도구 요청 시 상한(4회) 이후 `null` 반환
- `get_storage`/`get_wave_status`/`get_companion_status` 3종이 실제 `World` 상태(창고/웨이브/티모시 상태)를 정확히 읽어오는지
- 알 수 없는 도구 이름을 요청하면 예외 대신 에러 객체를 반환하는지

(결과: `corePersonaClient.test.ts` 26개 케이스, `companionTools.test.ts` 신설, `pnpm test`에 자동 편입 — 커밋 `8fc3b7b`)


### 사례 3. 오류 분석 및 수정 요청

#### 요청 목적
채팅으로 `@티모시 ...`를 연속으로 물어보면 두 번째 질문에 대한 답이 오지 않는 문제

#### 프롬프트
```text
[실제 대화 프롬프트 원문은 로컬 세션 기록에만 남아 있어 이 문서에는 없음 — 담당 팀원이 직접 채워 넣을 것]
```

#### 해결 과정
- 문제 상황: `playerMessageCooldownSeconds`(8초) 안에 다시 말을 걸면 `sendCompanionMessage`가 조용히 `false`를 반환하고 이벤트 자체를 버려서, 채팅 로그엔 질문이 보이는데 답은 영영 오지 않는 것처럼 보였습니다.
- 원인 분석: 쿨다운 중 들어온 질문을 큐에 남기지 않고 즉시 폐기하는 구조였습니다.
- 수정 내용: 쿨다운 중 질문을 버리지 않고 FIFO 큐(`queuedCompanionMessages`, 최대 3개 `MAX_QUEUED_COMPANION_MESSAGES`)에 저장한 뒤, 쿨다운이 끝나는 tick마다 가장 오래된 질문 하나씩 자동으로 내보내도록 수정(`tickQueuedCompanionMessages`). 큐 한도를 넘는 진짜 스팸은 여전히 거절합니다.
- 검증 결과: Vitest로 큐잉/쿨다운 종료 후 자동 처리/순서(FIFO) 유지/큐 초과 시 거절 케이스를 모두 검증 (`packages/shared/tests/ai/companionPersona.test.ts`, 커밋 `422eb84`)

### 사례 4. 멀티플레이 텔레포트 버그 진단 및 클라이언트 예측(prediction) 구현

#### 요청 목적
멀티플레이 중 캐릭터 화면이 정지했다가 한 번에 튀는(텔레포트) 현상의 원인을 규명하고 해결

#### 프롬프트 (실제 원문, 시간순 발췌)
```text
patch_rate를 40으로 되돌리면 너무 느린거같다니까 거의 도입할수없어 게임으로서
원활한  게임이 불가능해

그럼 어디서 더 최적화해야할까?

(구현·배포 후) 아직도 버벅이는 느낌이있는데 어떻게 최적화하거나 할방법있을까?

(재접속 실측 후) 방금 마지막 테스트에서 내 캐릭터가 미니맵상 원래 위치보다
한참 우측 거의 맵끝까지 갔다가 다시 원래 위치로 돌아오는 현상(렉같음)을 계속
경험했어 지금 열려있는 chrome 창 devtool에 전부 기록되어있고
```

#### 해결 과정
- 팀원이 공유한 원인 분석 문서를 코드와 직접 대조해 각 주장을 검증했습니다(카메라
  추적 계수(lerp=1), 보간 지연(≈33ms), `PATCH_RATE` 변경 이력 등).
- "PATCH_RATE를 낮춘다"는 미봉책은 위 프롬프트에서 명시적으로 기각됐습니다 —
  반응성과 네트워크 안전 여유가 같은 상수 하나에 묶여 있는 게 근본 문제였으므로,
  자기 캐릭터만이라도 네트워크 도착 시각과 분리하는 클라이언트 예측+재조정
  구조를 설계해 계획 승인을 받은 뒤 구현했습니다.
- `shared/sim`의 이동·충돌 로직을 서버와 클라이언트 예측이 공유하는 순수 함수
  (`resolvePlayerMove`, `isPlayerBlocked`)로 분리해, 두 쪽 판정이 갈라지지
  않게 했습니다.
- 구현 직후 자체 재검토 과정에서 재조정이 "보간된 좌표"와 "다른 시점의 seq"를
  짝지어 쓰던 미세 오차, 입력 전송 주기(60Hz)와 화면 주사율이 어긋나 계단식으로
  보이던 문제를 스스로 재현해 함께 수정했습니다.
- 배포 후 실사용 중 나온 "미니맵에서만 순간이동한다"는 제보는, 먼저 DevTools
  Network 패킷을 실측해 "네트워크 지연/정체" 가설부터 검증하고 기각한 뒤(측정한
  결과 패킷은 고른 간격으로 정상 도착) 코드를 다시 추적했습니다. 원인은
  `HudScene`(미니맵을 그리는 별도 Scene)이 `GameScene`의 예측 좌표를 전혀
  모른 채 여전히 보간된 좌표만 쓰고 있었던 것으로 확정했고, 기존 씬 간 데이터
  공유 패턴(`registry`)을 그대로 따라 고쳤습니다.

#### 검증 방법
- `pnpm --filter @dropfall/shared test`(회귀 500건 이상), `typecheck`, `pnpm lint`를
  변경마다 재실행.
- Playwright로 실제 배포 서버에 접속해 이동·충돌 스모크 테스트를 직접 수행.
- 미니맵 수정은 버그 자체가 네트워크 지터가 있어야 드러나는 것이라 지터가 없는
  로컬 환경에서는 "배선이 정상 동작한다"까지만 확인했고, **실제 재현 여부는
  다음 실접속 테스트가 필요하다고 보고서에 명시적으로 남겼습니다**(검증 없이
  "해결됨"이라 단정하지 않기 — §2.3 규칙).

#### 관련 자료
- 커밋: `3df2850`(feat: 자기 캐릭터 클라이언트 예측+재조정), `0bff528`(fix: 미니맵
  순간이동 수정)
- 작업 보고서: `docs/backend/55-work-report-player-client-prediction.md`(§1~8,
  조사→구현→후속 버그 수정 3건까지 전 과정 기록)

## 3. 에셋 제작 과정에서의 AI 활용

게임에 들어가는 그림은 성격이 서로 달라서, **한 가지 방법으로 전부 만들 수 없었습니다.** 그래서 그림의 성격에 따라 두 가지 경로로 나누어 작업했습니다.

| 그림의 성격 | 제작 방식 | 해당 에셋 |
|---|---|---|
| 형태와 분위기가 중요한 **한 장짜리 큰 그림** | 이미지 생성 AI(Nano Banana)로 초안 생성 → Aseprite에서 직접 편집 | 랜딩·로비 배경, 행성 이미지, 로고, 9-slice UI 프레임 |
| 규칙적인 변화가 전부인 **반복·절차적 그림** | AI가 Aseprite Lua 스크립트 작성 → Aseprite CLI로 실행 | 지형 타일셋, 이펙트(FX) 12종, 건축물 티어별 변형, HUD 부품 |

### 3.1 이미지 생성 AI + 수작업 편집 (배경·UI 이미지)

배경처럼 **한 번 만들고 오래 쓰는 큰 그림**은 절차적으로 만들 이유가 없고, 손으로 처음부터 그리기에는 비용이 큽니다. 이 경우 Nano Banana로 초안을 만든 뒤 Aseprite로 직접 편집하는 방식을 썼습니다.

**작업 흐름**

1. 원하는 분위기와 제약(해상도, 색조, 화면에서 차지할 위치)을 프롬프트로 정리해 이미지를 생성합니다.
2. 생성 결과를 Aseprite로 불러와 **직접 편집**합니다. 이 단계가 반드시 필요했습니다 — 생성 이미지는 픽셀 그리드에 맞지 않고 색 수가 과도하게 많아서, 그대로 쓰면 게임의 픽셀아트 톤에서 확연히 튀기 때문입니다.
3. 색 수를 프로젝트 공용 팔레트(`assets/palette/dropfall.gpl`, 32~48색) 수준으로 줄이고, 경계를 픽셀 단위로 정리하고, 화면에서 잘릴 영역을 고려해 구도를 조정합니다.
4. `assets/ui/backgrounds/`, `assets/ui/dom/` 등 규약 위치에 저장하면 빌드가 산출물로 복사합니다.

**실제 프롬프트 예시 — 로비 배경**

```text
탑다운 2D 픽셀아트 생존 게임의 로비 화면 배경.
어두운 우주 정거장 내부에서 창밖으로 황폐한 행성이 보이는 구도.
전체적으로 어둡고 채도가 낮은 청록/남색 계열 — 위에 올라갈 초록색 홀로그램
UI 텍스트가 묻히지 않아야 한다.
화면 중앙은 UI 패널이 덮으므로 시선을 끄는 요소를 두지 말 것.
16:9 비율, 가장자리가 잘려도 무방하도록 중요한 요소는 중앙에서 벗어나게 배치.
```

프롬프트에 **"UI가 위에 올라간다"는 제약을 명시한 것**이 핵심이었습니다. 이 조건 없이 생성하면 배경 자체는 멋있지만 중앙에 밝은 요소가 몰려 UI 글자가 읽히지 않았습니다. 배경은 주인공이 아니라 바탕이라는 조건을 프롬프트가 알고 있어야 합니다.

### 3.2 AI 생성 Lua 스크립트 + Aseprite CLI (타일·이펙트)

이펙트와 타일은 "밝은 중심 → 따뜻한 색 → 소멸"처럼 **규칙적인 변화가 전부**라 수식으로 표현됩니다. 프레임마다 손으로 찍으면 타이밍을 한 번 바꿀 때마다 전부 다시 그려야 하지만, 스크립트는 상단 파라미터만 고치면 됩니다. 그래서 AI에게 **그림이 아니라 그림을 그리는 프로그램**을 작성하게 했습니다.

**작업 흐름**

1. 팀이 필요한 이펙트의 사양(캔버스 크기, 프레임 수, 좌표 규약, 색 제한)을 정의합니다.
2. Claude Code가 Aseprite Lua API로 픽셀을 찍는 생성 스크립트를 `assets/_generators/`에 작성합니다.
3. Aseprite CLI를 배치 모드로 실행해 `.aseprite` 원본을 생성합니다.
4. `preview.lua`로 프레임을 가로로 이어 붙이고 **실제 게임의 밤 배경색을 깔아** 확대 저장한 뒤 눈으로 확인합니다(투명 배경 그대로 보면 밝은 이펙트가 흰 바탕에 묻혀 판단이 안 됩니다).
5. 마음에 들지 않으면 프롬프트로 방향을 지시해 파라미터를 조정하고 2~4를 반복합니다.
6. `pnpm build:atlas`로 아틀라스를 리빌드하면 코드 수정 없이 게임에 반영됩니다.

```bash
ASE="C:/Program Files (x86)/Steam/steamapps/common/Aseprite/Aseprite.exe"

# 생성
"$ASE" -b --script-param out="$(pwd)/assets/sprites/fx/fx_swing.aseprite" \
       --script assets/_generators/fx_swing.lua

# 확인 (밤 배경 위에 프레임을 이어 붙여 3배 확대)
"$ASE" -b --script-param src="$(pwd)/assets/sprites/fx/fx_swing.aseprite" \
       --script-param out="$(pwd)/preview.png" --script-param scale=3 \
       --script assets/_generators/preview.lua

# 아틀라스 리빌드
pnpm build:atlas
```

**실제 프롬프트 예시 — 근접 무기 휘두르기 이펙트 재작업**

첫 결과물은 4색 띠를 두껍게 겹쳐 쌓아 픽셀아트라기보다 에어브러시처럼 뭉툭했습니다. 이때 "더 예쁘게"가 아니라 **픽셀아트의 문법을 지목해서** 다시 지시한 것이 결정적이었습니다.

```text
지금 휘두르기 이펙트가 픽셀아트가 아니라 에어브러시처럼 뭉툭하다.
Dead Cells, Katana Zero 계열의 베기 문법으로 다시 그려라.

1. 얇고 또렷하게 — 본체는 배에서 3~4px, 양끝은 1px로 좁아지는 초승달 하나.
   색은 3개(흰 심 + 하늘색 + 어두운 강청)만 쓰고 그라데이션 층을 쌓지 마라.
   하드 엣지가 곧 픽셀 감성이다.
2. 에코 선 — 본체 안쪽에 1px짜리 짧은 호를 한 줄 따라 붙여라. 잔상 두 겹이
   "빠르게 지나갔다"를 말해준다.
3. 파편 소멸 — 끝날 때 통째로 사라지지 말고 호가 짧은 조각으로 부서졌다가
   낱알 픽셀로 흩어지게. 마지막에 날 끝 자리에 작은 + 스파클 하나.
```

이 지시가 그대로 스크립트의 설계 주석과 파라미터로 남아 있습니다(`assets/_generators/fx_swing.lua`). **"왜 이렇게 그렸는가"가 코드에 남으므로**, 나중에 다른 이펙트를 만들 때 같은 규칙을 이어서 적용할 수 있습니다.

**실제 프롬프트 예시 — 바닥 스파이크 3티어**

새 게임 기능(바닥 스파이크)을 추가하면서 그림도 함께 만든 사례입니다. 게임 규칙이 곧 그림의 제약이 되었습니다.

```text
설치물에 바닥 스파이크를 티어별로 추가한다 — 나무/돌/철.
마찬가지로 lua 스크립트로 픽셀아트 만들어서 구현 진행해줘라.
```

AI가 기존 에셋을 측정해 규칙을 스스로 맞췄습니다. 벽·울타리와 **같은 타일 발자국**(x 7~23, 바닥선 y 28)을 쓰고, 재질 색은 기존 `buildings_tiers.lua`의 돌·철 팔레트를 그대로 가져왔습니다 — 같은 재질이 화면에서 같은 색이어야 배치만 보고 티어를 읽을 수 있기 때문입니다. 또한 가시 끝이 y 20 위로 올라가지 않게 제한했는데, **스파이크는 막는 물건이 아니라 밟는 물건**이라 지나가는 캐릭터를 가리면 안 되기 때문입니다.

### 3.3 이 방식에서 지킨 규칙

에셋 작업에서도 몇 가지 규칙을 명시적으로 정했습니다.

- **색 수는 4색 안팎으로 제한합니다.** 그라데이션을 늘리면 픽셀아트로 읽히지 않고 뿌옇게 뭉갭니다.
- **좌표 규약을 통일합니다.** 방향이 있는 이펙트는 전부 `+x`가 진행 방향이고, 렌더러가 조준각만큼 회전시킵니다.
- **이펙트 크기는 실제 판정 범위와 맞춥니다.** 예를 들어 휘두르기 호는 반경 18~30px 구간에 그리는데, 이는 서버의 공격 판정 범위와 겹치도록 맞춘 값입니다 — 닿아 보이는 곳이 실제로 맞는 곳이어야 합니다.
- **생성 결과는 반드시 눈으로 확인한 뒤 반영합니다.** 스크립트가 오류 없이 실행되는 것과 그림이 쓸 만한 것은 별개입니다.

### 3.4 이 방식의 한계

- **캐릭터·몬스터처럼 형태가 중요한 그림에는 맞지 않습니다.** 절차적 생성은 규칙적인 변화에만 유효하며, 실제로 몬스터 스프라이트는 외부 상용 에셋을 구매해 사용했습니다(§8.2).
- **이미지 생성 AI의 결과물은 그대로 쓸 수 없습니다.** 픽셀 그리드 정렬과 색 수 축소를 위한 수작업 편집이 항상 필요했습니다.

## 4. 게임 내 AI 활용

DropFall은 단순한 규칙 기반 NPC가 아닌, LLM API와 Tool Calling을 활용하는 AI 동료를 게임 내에 적용했습니다.

AI 동료(티모시)의 자원 탐색·채집 행동 자체는 규칙 기반 상태머신(`seeking → traveling → harvesting → returning → depositing`)으로 자동 수행되며 LLM은 관여하지 않습니다.
대신 플레이어가 채팅으로 `@티모시`를 멘션하면, 그 순간에만 LLM이 게임 상태 조회용 Tool을 호출해 실제 수치(창고 자원량, 웨이브 상태, 티모시 자신의 상태)를 확인한 뒤 캐릭터로서 자연어로 답합니다.
플레이어가 몬스터 처치 및 코어 방어에 집중하는 동안, AI 동료는 자원 수급을 지원하고 필요할 땐 대화 상대가 되어 줍니다.

AI 동료의 채팅 응답은 LLM이 대화 맥락과 역할 지시를 바탕으로 필요한 Tool을 선택하고,
서버가 허용된 Tool 이름인지 검증한 뒤 실행 결과를 프롬프트에 다시 반영하는 방식으로 구현했습니다.

### 4.1 현재 구현된 AI 동료 기능

| 기능 | 설명 | 현재 구현 여부 |
|---|---|---|
| 자원 탐색·채집 | 전장의 자원 노드를 찾아 이동·채집·코어 창고 반납까지 자동 수행 (규칙 기반 상태머신, LLM 미관여) | 구현 |
| 게임 상태 인식(채팅 질의 시) | `@티모시` 멘션 시 창고 자원량·웨이브 상태·자신의 hp/보유자원/행동 상태를 Tool Calling으로 조회 | 구현 |
| LLM API 호출 | 서버가 Anthropic Messages API를 호출해 대사/답변 생성 (이벤트 발생 시, 채팅 멘션 시) | 구현 |
| Tool Calling | 정의된 3개 읽기 전용 도구를 LLM이 호출하고, 서버가 검증 후 실행 | 구현 |
| 자연어 채팅 질의응답 | 플레이어가 `@티모시 <질문>`으로 물으면 LLM이 실제 게임 상태를 조회해 자연어로 답변 | 구현 |
| 전투 참여 | - | 향후 계획 |
| 자연어 채팅 행동 지시 (예: "저기 가서 채집해") | 현재는 질의응답만 가능, 행동 지시·실행은 미구현 | 향후 계획 |


## 5. AI 동작 구조

AI 동료의 채팅 응답은 다음 흐름으로 처리됩니다. (자원 탐색·채집은 이 흐름과 별개로 규칙 기반 로직이 매 tick 처리합니다.)

1. 플레이어가 채팅에 `@티모시 <질문>`을 입력합니다.
2. 서버(`World.sendCompanionMessage`)가 멘션을 감지하고, 쿨다운(8초)을 확인한 뒤 이벤트를 발생시킵니다.
3. 서버가 역할 프롬프트, 현재 무드(trust/efficiency/recklessness), 최근 대화 기록(최대 6턴), 사용 가능한 Tool 목록을 구성해 LLM API에 전달합니다.
4. LLM은 답변에 필요한 실제 수치가 있으면 Tool Calling 응답을 반환합니다.
5. 서버는 호출된 Tool 이름이 허용 목록(3개) 안에 있는지 검증한 뒤 실행하고, 결과를 다시 LLM에 돌려줍니다(최대 4회 왕복).
6. LLM은 Tool 실행 결과를 반영해 최종 자연어 대사를 생성합니다.
7. 서버가 생성된 대사를 게임 상태(채팅 로그/말풍선)에 반영하고, 클라이언트 화면에 표시합니다. API 실패·타임아웃(5초) 시에는 폴백 대사로 대체합니다.

### 구조도
```
[게임 클라이언트 (Phaser 3)]
  - 플레이어 조작(이동/공격/채팅)
  - "@" + Tab 티모시 멘션 자동완성
  - AI 동료 행동/대사 표시
            │  WebSocket (Colyseus 상태 동기화)
            ▼
[게임 서버 (Colyseus Room, 서버 권위)]
  - 자원 채집: 규칙 기반 상태머신 (LLM 비관여, 매 tick 처리)
  - 채팅에서 "@티모시 ..." 감지 시에만 아래 AI 로직 트리거
            │
            ▼
[AI 페르소나 로직 (corePersonaClient.ts / companionPersona.ts)]
  - 역할 프롬프트 + 무드(trust/efficiency/recklessness) + 대화 기록 구성
  - 사용 가능 Tool 목록 첨부
            │
            ▼
[Anthropic Messages API (claude-haiku-4-5)]
  - 자연어 응답 또는 Tool Calling 응답 생성
            │
            ▼
[Tool 실행 및 검증 (companionTools.ts)]
  - get_storage / get_wave_status / get_companion_status (읽기 전용, 화이트리스트 3종)
  - 최대 4회 왕복 제한, 5초 타임아웃
            │
            ▼
[게임 상태 갱신]
  - 생성된 대사를 채팅/말풍선에 브로드캐스트
  - 실패 시 폴백 대사로 대체
```


## 6. LLM API 및 Tool Calling 구현

### 6.1 사용 모델 및 연동 환경

| 구분 | 내용 |
|---|---|
| LLM 제공사 | Anthropic |
| 사용 모델 | claude-haiku-4-5 (기본값, 환경변수 `CORE_PERSONA_MODEL`/`H_CHAT_API_MODEL`로 변경 가능) |
| API 호출 위치 | 서버 (`packages/server/src/persona/corePersonaClient.ts`, Node 내장 `fetch`로 직접 호출) |
| 게임 서버 기술 | Node.js + Colyseus(실시간 상태 동기화) + Express |
| 클라이언트 기술 | Phaser 3 (TypeScript, Vite 빌드) |
| 통신 방식 | WebSocket (Colyseus Schema 기반 상태 델타 동기화) |

API는 두 경로 중 하나로 호출합니다: Anthropic API 직접 호출(`https://api.anthropic.com/v1/messages`) 또는 사내 게이트웨이(hchat) 경유. 어느 쪽을 쓸지는 설정된 환경변수(API 키)에 따라 자동으로 결정됩니다.

### 6.2 현재 구현된 Tool 목록

| Tool 이름 | 역할 | 입력값 | 반환값 또는 실행 결과 |
|---|---|---|---|
| `get_storage` | 코어 창고에 지금 쌓여 있는 자원(나무/돌/부품) 개수 확인 | 없음 | `{ wood, stone, parts }` |
| `get_wave_status` | 현재 몇 번째 웨이브인지, 낮/밤 중 어느 쪽인지, 남은 시간 확인 | 없음 | `{ wave, phase, phaseTimeRemainingSeconds }` |
| `get_companion_status` | 티모시 자신의 체력, 보유 자원, 현재 행동(채집 중/이동 중/다운 등) 확인 | 없음 | `{ hp, maxHp, carriedWood, carriedStone, state }` |

세 도구 모두 **읽기 전용**이며 게임 상태를 변경하지 않습니다. 이름 그대로 "질문에 답하기 위한 조회"만 수행합니다(`packages/server/src/persona/companionTools.ts`).


### 6.3 Tool Calling 처리 방식

LLM은 게임 상태를 직접 수정하지 않습니다.
LLM은 사전에 정의된 Tool을 호출할 수 있으며, 실제 게임 상태 변경은 서버의 검증 로직을 통과한 경우에만 수행됩니다.

| 단계 | 처리 내용 |
|---|---|
| 1 | 게임 상태 및 AI 역할 정보를 LLM에 전달 |
| 2 | LLM이 필요 시 Tool Calling 응답 생성 |
| 3 | 서버가 호출 Tool 및 인자값 검증 |
| 4 | 게임 규칙에 부합하는 경우 Tool 실행 |
| 5 | 실행 결과를 게임 상태에 반영 |
| 6 | 결과를 다음 AI 판단 또는 클라이언트 화면에 활용 |


## 7. AI 대상 주요 프롬프트 및 지시 사항

### 7.1 AI 동료 역할 지시

티모시(AI 동료) 시스템 프롬프트 (`packages/shared/src/sim/companionPersona.ts`):
```text
너는 생존 디펜스 게임 DropFall의 AI 동반자 "티모시"다. 자원을 채집해 나르는
작은 로봇이자 팀의 마스코트다. 지금 말을 거는 대상 플레이어와의 관계에 따라 성격이 변한다 —
지금 그 플레이어를 향한 무드는 "{mood}"(trust={trust}, efficiency={efficiency}, recklessness={recklessness}).
한국어로, 짧게 한 문장만 대사로 말해라. 설명이나 따옴표 없이 대사 자체만 출력해라.
```
채팅 멘션(`@티모시 ...`)일 때는 이어서 유저 메시지로 다음을 덧붙여 전달합니다(직전 대화 기록 최대 6개도 함께 첨부):
```text
이 플레이어가 채팅으로 너에게 직접 말을 걸었다: "{실제 메시지}". 그 말에 대답해줘.
```

코어(중앙 코어) 시스템 프롬프트 (`packages/shared/src/sim/corePersona.ts`, Tool Calling 없이 대사만 생성):
```text
너는 생존 디펜스 게임 DropFall의 중앙 코어다. 불시착한 생존자들이 구조 신호를 보내려고
지키고 있는 장치이자 유일한 말동무다. 플레이어들의 행동에 따라 성격이 변한다 — 지금 네
무드는 "{mood}"(trust={trust}, efficiency={efficiency}, recklessness={recklessness}).
한국어로, 짧게 한 문장만 대사로 말해라. 설명이나 따옴표 없이 대사 자체만 출력해라.
```

두 페르소나 모두 `trust`/`efficiency`/`recklessness`(-10~10) 3개 수치로 무드(warm/cold/neutral)를 계산하며, 이벤트(코어 납품, 근접 상호작용, 다운, 부활, 웨이브 종료, 채팅 등)마다 값이 조금씩 바뀝니다.

### 7.2 LLM에 전달하는 게임 상태

| 상태 정보 | 전달 방식 |
|---|---|
| AI 동료 위치 | 전달 안 함 |
| 자원 위치 | 전달 안 함 |
| 자원 상태(수량 제외 위치/거리 등) | 전달 안 함 |
| AI 동료 상태(체력/보유자원/행동상태) | Tool 조회 (`get_companion_status`, 필요 시에만) |
| 보유 자원(창고 기준) | Tool 조회 (`get_storage`, 필요 시에만) |
| 웨이브/낮밤/남은 시간 | Tool 조회 (`get_wave_status`, 필요 시에만) |
| 무드/관계 수치(trust·efficiency·recklessness), 웨이브 번호, 이벤트 종류, 채팅 메시지·대화 기록(최대 6턴) | 프롬프트에 상시 포함 |

AI 동료의 좌표, 자원 노드 위치, 몬스터 위치, 코어 HP는 프롬프트에도 Tool 결과에도 포함되지 않습니다.

### 7.3 안전 및 제약 조건

AI 동료가 게임 규칙을 벗어난 행동을 하지 않도록 다음 제약을 적용했습니다.

- LLM은 정의된 3개 Tool(`get_storage`/`get_wave_status`/`get_companion_status`) 외의 어떤 게임 기능도 직접 호출할 수 없습니다.
- 세 Tool은 모두 읽기 전용이라, LLM이 게임 상태를 변경하는 것 자체가 구조적으로 불가능합니다.
- 알 수 없는 도구 이름이 요청되면 예외를 던지지 않고 에러 객체를 반환해 응답 루프가 깨지지 않게 합니다.
- Tool 왕복 횟수를 4회로 제한해 모델이 도구 호출을 무한 반복하지 못하게 합니다.
- API 요청에 5초 타임아웃을 두고, 실패·타임아웃 시 항상 폴백 대사로 대체하여 API 장애가 "AI가 응답하지 않음"으로 보이지 않게 합니다.
- 채팅 멘션 응답에는 8초 쿨다운을 적용하고, 쿨다운 중 들어온 질문은 버리지 않고 큐(최대 3개)에 쌓아 순서대로 처리합니다.


## 8. 향후 확장 계획

현재 AI 동료는 자원 탐색·채집(규칙 기반)과 채팅 질의응답(LLM Tool Calling)을 수행합니다.
향후에는 현재의 LLM API 및 Tool Calling 구조를 기반으로 다음 기능을 확장할 계획입니다.

- 이동, 전투, 방어, 아이템 사용 등 AI 행동을 실제로 변경하는 Tool 추가 (현재는 읽기 전용 조회 Tool만 존재)
- 플레이어가 채팅을 통해 AI 동료에게 자연어로 행동을 지시하는 기능
- 코어 체력, 몬스터 위치, 자원 분포 등을 고려한 상황 인식 고도화(현재는 프롬프트/Tool 어디에도 포함되지 않음)
- 외부 API 의존도를 줄이기 위한 Local LLM 적용 검토

위 기능은 현재 구현된 기능과 구분되는 **향후 개발 계획**입니다.

## 9. 외부 에셋 및 오픈소스 출처

### 9.1 오픈소스 및 라이브러리

| 분류 | 이름 | 사용 목적 | 라이선스 | 출처 링크 |
|---|---|---|---|---|
| 게임 엔진 | Phaser | 클라이언트 렌더링/입력/카메라/타일맵 | MIT | https://phaser.io/ |
| 클라이언트 빌드 도구 | Vite | 클라이언트 빌드/개발 서버 | MIT | https://vitejs.dev/ |
| 실시간 멀티플레이 프레임워크 | Colyseus (colyseus, @colyseus/schema, @colyseus/ws-transport, @colyseus/sdk) | 서버 권위 방(Room) 관리, 상태 델타 동기화, WebSocket 전송, 클라이언트 접속 | MIT | https://colyseus.io/ |
| 백엔드 프레임워크 | Express | 서버 HTTP 엔드포인트 | MIT | https://expressjs.com/ |
| 데이터 검증 | zod | 밸런스 데이터(JSON) 런타임 스키마 검증 | MIT | https://zod.dev/ |
| 테스트 프레임워크 | Vitest | 유닛/통합 테스트 | MIT | https://vitest.dev/ |
| 폰트 | Galmuri | 한글 픽셀 폰트(게임 UI·HUD) | SIL Open Font License 1.1 | https://github.com/quiple/galmuri |
| LLM API | Anthropic Claude API (claude-haiku-4-5) | AI 동료/코어 페르소나 대사 생성 및 Tool Calling | Anthropic 서비스 이용약관 | https://www.anthropic.com/ |

### 9.2 외부 그래픽 에셋

**적 몬스터 및 보스 몬스터 스프라이트**

| 항목 | 내용 |
|---|---|
| 에셋명 | Tiny RPG Character Asset Pack v1.2 (Free Sprites 포함) |
| 제작자 | Zerie |
| 사용 위치 | 적 몬스터 및 보스 몬스터 스프라이트 (`assets/sprites/monsters/`, `monsters` 아틀라스) |
| 출처 | https://zerie.itch.io/tiny-rpg-character-asset-pack-02 |

라이선스 조건은 다음과 같습니다.

```text
License:
✔️ Allowed
  You may use these assets in both personal and commercial video game projects.
  You may edit or modify these assets to fit your game projects.

❌ Not Allowed
  You may not redistribute, resell, or re-upload these assets,
  whether modified or unmodified.
  You may not use these assets for AI training or NFT projects.
```

**라이선스 준수를 위해 적용한 조치**

- **재배포 금지 조항 준수:** 원본과 빌드 산출물 모두 공개 저장소에 커밋하지 않습니다. 비공개 저장소(`CKCdefense/dropfall-assets`)에 보관하고, GitHub Actions 배포 빌드 시점에만 완성된 아틀라스를 주입합니다(`.github/workflows/deploy-pages.yml`). 저장소를 공개해도 에셋 파일 자체는 포함되지 않습니다.
- **AI 학습 금지 조항 준수:** 본 에셋은 어떠한 AI 모델의 학습·파인튜닝에도 사용하지 않았으며, 이미지 생성 AI의 입력으로도 사용하지 않았습니다. §3에서 설명한 AI 에셋 제작(Nano Banana 생성, Lua 절차적 생성)은 **몬스터 스프라이트와 무관한 별개의 에셋**(배경, 타일, 이펙트, UI)에만 적용했습니다.
- **수정 허용 조항 활용:** 게임의 어두운 타일 팔레트에 맞추기 위한 **색 조정에 한해** 수정했습니다(`assets/_generators/monsters_restyle.lua` — 색상환 회전, 채도·명도 배율, 대비, 그림자 한색화). 형태·프레임 수·애니메이션 태그는 원본 그대로 두므로 공격 모션 타이밍에는 영향이 없습니다.
- **변환 과정만 커밋:** 재배포 금지 조항을 지키기 위해 원본 팔레트를 하드코딩하지 않고 **색 연산만** 스크립트에 담았습니다. 결과물은 커밋하지 않고 변환 과정만 공개 저장소에 남습니다(`assets/sprites/monsters/README.md`).

### 9.3 외부 사운드 에셋

**현재 사운드/BGM은 실제로 게임에 연결되어 있습니다**(`docs/backend/75-work-report-audio-system.md`). 팀원이 저장소 루트 `sounds/`에 미리 확보해 둔 원본 오디오 중 실제로 쓰는 23개(효과음 17 + 배경음악 6)를 골라 `packages/client/public/assets/sounds/`로 정리하고, `packages/client/src/game/audio/AudioManager.ts`가 국면별 배경음악(낮/밤/보스전 크로스페이드)·지형별 발소리·전투음·부활/쓰러짐·몬스터 스폰·재장전 이벤트에 연결했습니다.

| 구분 | 내용 |
|---|---|
| 효과음(17개) | 플레이어(다운/부활/발소리 3종), 근접 전투, 원거리 총성 3종, 몬스터 스폰/울음 3종 |
| 배경음악(6개) | 낮 1곡, 밤 1곡, 보스전 4곡(2~5일차 보스별) |
| 코드 연결 | `packages/client/src/game/audio/AudioManager.ts`(신규), `InputController.ts`(빈 탄창 훅), `GameScene.ts`(프리로드·음소거·배선) |

**출처·라이선스 확인 필요**: 이 오디오 파일들은 각 사이트에서 직접 검색해 다운로드한
것이 아니라 팀원이 사전에 확보해 저장소에 커밋해 둔 것이라, 파일 단위의 정확한
출처·제작자·라이선스 조건이 이 문서에 아직 정리되어 있지 않습니다. 제출 전 팀
내부에서 원 출처를 재확인해 아래 표를 채워 넣어야 합니다.

| 에셋명 | 제작자 | 사용 위치 | 출처 | 라이선스 |
|---|---|---|---|---|
| (담당 팀원이 원 출처를 확인해 채워 넣을 것) | - | `packages/client/public/assets/sounds/` | - | - |

**검토했으나 실제로는 쓰지 않은 후보**: 무료 CC0 소스(Kenney.nl, Freesound, OpenGameArt, Pixabay, Sonniss GDC 번들)를 대안으로 조사했으나, 팀이 이미 확보해 둔 오디오로 충분해 실제 게임에는 반영하지 않았습니다.

### 9.4 출처 사이트

실제 사용 중인 에셋 출처:

- itch.io (Zerie — Tiny RPG Character Asset Pack): https://zerie.itch.io/tiny-rpg-character-asset-pack-02
- Galmuri (한글 픽셀 폰트): https://github.com/quiple/galmuri

사운드 작업 시 후보로 검토한 사이트(현재 실제로 사용 중인 에셋은 없음):

- Kenney: https://kenney.nl/
- Freesound: https://freesound.org/
- OpenGameArt.org: https://opengameart.org/
