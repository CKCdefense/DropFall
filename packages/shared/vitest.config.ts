import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    /**
     * 기본값 5초는 이 저장소에 짧다.
     *
     * 시뮬레이션 테스트는 수백~수천 틱을 돌리는데, 개별 테스트는 단독으로 재면
     * 100ms 안팎이다(가장 무거운 건축물 파괴 테스트가 73ms). 문제는 vitest가 파일을
     * 여러 워커로 병렬 실행한다는 점이다 — 다른 작업이 CPU를 함께 쓰는 상황에서
     * 워커들이 서로 밀리면 100ms짜리가 5초를 넘겨 "Test timed out"으로 죽는다.
     * 실제로 14회 연속 실행 중 1회가 이렇게 실패했다(어서션이 아니라 타임아웃).
     *
     * 20초로도 모자란 경우가 남았다 — World를 수십 번 **새로 만드는** 테스트들이다
     * (콜로니 배치 20회, 상점 로테이션 200회 등). 생성자가 지형·자원 배치까지 도는
     * 무거운 작업이라, 단독으로 십수 초 걸리던 것이 병렬 부하에서 20초를 넘겼다.
     * 실제로 콜로니·티모시·상점 테스트가 번갈아 이렇게 실패했다(전부 어서션이 아니라
     * 타임아웃이고, 단독으로 돌리면 통과한다).
     *
     * 60초면 그 부류까지 여유가 생기면서, 진짜 무한 루프는 여전히 잡힌다 — 정상
     * 테스트(100ms 안팎)와 두 자릿수 배수 이상 떨어져 있다. 개별 테스트에 더 긴
     * 값을 적어 둔 곳이 있는데(colony/economy), 그건 "이 테스트가 특히 무겁다"는
     * 표시라 그대로 둔다.
     */
    testTimeout: 60_000,
  },
});
