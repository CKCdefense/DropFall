import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    /**
     * 기본값 5초는 이 저장소에 짧다.
     *
     * 시뮬레이션 테스트는 수백~수천 틱을 돌리는데, 개별 테스트는 단독으로 재면
     * 100ms 안팎이다(가장 무거운 건축물 파괴 테스트가 73ms). 문제는 vitest가 파일을
     * 여러 워커로 병렬 실행한다는 점이다 — 다른 작업이 CPU를 함께 쓰는 상황에서
     * 워커들이 서로 밀리면 100ms짜리가 5초를 넘겨 "Test timed out"으로 죽는다.
     * 실제로 14회 연속 실행 중 1회가 이렇게 실패했다(어서션이 아니라 타임아웃).
     *
     * 20초면 CPU 경합으로 인한 헛된 실패는 사라지면서, 진짜 무한 루프는 여전히
     * 잡힌다 — 정상 테스트와 두 자릿수 배수만큼 떨어져 있다.
     */
    testTimeout: 20_000,
  },
});
