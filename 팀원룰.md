# DropFall 팀원 롤 기술서

- 게임명: DropFall
- 팀명: DropFall
- 팀 구성: 3인
- GitHub Repository: https://github.com/CKCdefense/DropFall

## 1. 팀 구성 및 역할 요약

| 팀원 | 담당 역할 | 주요 담당 영역 |
|---|---|---|
| 김동현| 백엔드 / 게임 시뮬레이션 개발 | 서버 권위 게임 로직(전투·웨이브·보스), 코어 경제(자원·에너지), 부활·콜로니 시스템, 사운드 통합, CI/CD 배포 |
| 최두용 | [예: AI 에이전트 개발] | AI 동료 생성, tool-calling, LLM api 연결 |
| 추보문 | 클라이언트 / UI·아트 개발 | Phaser 클라이언트 아키텍처, 인게임 HUD·모달 UI 전체, 픽셀아트 에셋 파이프라인, 네트워크 보간·클라이언트 예측, 전투·연출 렌더링 |

## 2. 팀원별 역할 및 구현 내용

### 김동현 — 백엔드 / 게임 시뮬레이션 개발

#### 담당 역할
- 서버 권위(server-authoritative) 게임 시뮬레이션 설계 및 구현 — 클라이언트는 렌더링만
  담당하고, 이동·전투·경제 판정은 전부 서버(`World`)가 검증한다.
- 전투·웨이브·코어 경제·부활·콜로니 등 핵심 게임 규칙을 기획하고 그대로 구현까지 맡았다.
- Colyseus 실시간 멀티플레이 서버와 배포 파이프라인(CI/CD) 구축.
- 플레이 중 나온 피드백·버그 제보를 받아 밸런스·로직을 반복적으로 수정.

#### 직접 구현한 영역
- **서버 아키텍처**: Colyseus 방(Room) + 순수 TypeScript 시뮬레이션 코어(`shared/sim`)
  분리 설계. 20Hz 틱 동기화, 클라이언트 예측·재조정(지연 은닉).
- **전투 시스템**: 근접/원거리 무기 판정, 무기별 총알 관통(pierce)·연사속도·재장전,
  보스 전용 공격 패턴(돌진 예고·광역 예고).
- **몬스터 AI**: Flow Field 기반 경로탐색, 공간분할 격자(SpatialGrid)로 다수 개체
  충돌 연산 최적화, 장애물 회피(축 슬라이딩·접선 미끄러짐·탈출 점프), 웨이브 스폰
  (사방 동시 스폰, 인원수 스케일링), 보스 4종.
- **콜로니(몬스터 둥지)**: 트리클 스폰, 정화 보상, 원거리 무기 악용 버그 수정.
- **코어 경제 및 건축**: 자원·에너지 게이지, 충전, 강화, 부분 수리, 울타리/벽
  건축과 하드 충돌 판정.
- **부활 시스템**: 다운/유령 상태 전이, 동료 구조(홀드), AID 즉시 부활, 코어에서
  자원을 치르는 유령 부활.
- **직업 밸런싱**: 4개 직업(병사/탐색꾼/의무병/엔지니어) 고유 능력 설계·구현.
- **게임 사운드 시스템**: 발소리(지형별)·전투·몬스터·부활 효과음과 국면별(낮/밤/
  보스) 배경음악 지연 로드·크로스페이드·더킹(음악이 효과음을 묻는 문제 해결).
- **CI/CD**: GitHub Pages(클라이언트) / 개인 서버 SSH(게임 서버) 자동 배포 파이프라인.
- **버그 수정**: 몬스터가 코어를 뚫고 지나가는 충돌 버그, 콜로니 트리클 스폰 회귀,
  멀티플레이 몬스터 애니메이션 동기화 등을 재현 테스트로 먼저 확인한 뒤 수정.

#### 담당 결과 및 기여
서버 권위 아키텍처와 전투·웨이브·경제·부활 시스템을 설계·구현해, 여러 명이 동시에
접속해도 판정이 어긋나지 않는 실시간 협동 디펜스 루프를 완성했다. 버그를 고칠 때마다
"고치기 전 코드로 먼저 재현 → 수정 → 재검증" 순서를 지켜 회귀 테스트를 계속 쌓았고
(공유 시뮬레이션 코어 기준 500개 이상), 기능을 추가할 때마다 기존 동작이 깨지지
않았는지 매번 전체 테스트로 확인했다.

#### 확인 가능 자료
- GitHub: `packages/server/`(Colyseus 방), `packages/shared/src/sim/`(게임 시뮬레이션),
  `packages/shared/tests/`(회귀 테스트)
- 작업 보고서: `docs/backend/00~76번` — 작업마다 기획·과정·결과를 남겼다.
- 커밋 메시지: 무엇을 왜 바꿨는지 전부 한국어로 서술.


### 최두용 — AI 에이전트 및 백엔드 개발

#### 담당 역할
- 게임 내 AI 동료 행동 구조 설계
- LLM API 연동 및 Tool Calling 처리 로직 구현
- 상태 관리 / API 설계 및 구현

#### 직접 구현한 영역
- AI 동료의 자원 탐색 및 채집 행동 설계
- Claude Code API 연동
- AI 역할 프롬프트 및 게임 상태 전달 구조 구현
- Tool Calling 요청 파싱 및 실행 로직 구현
- [실제 Tool 이름] Tool 구현
- Tool 실행 결과의 게임 상태 반영 및 검증 로직 구현
- [테스트 코드, 배포, 오류 대응 등 실제 작업 추가]

#### 담당 결과 및 기여
LLM API와 Tool Calling을 기반으로 AI 동료가 게임 내 자원 탐색 및 채집 행동을 수행하도록 구현했습니다.
또한 LLM이 게임 상태를 직접 변경하지 않고, 사전에 정의된 Tool과 검증 로직을 통해서만 행동하도록 구성했습니다.

#### 확인 가능 자료
- GitHub: [AI 관련 모듈 경로]
- 주요 커밋: [커밋 메시지 또는 PR 링크]

### 추보문 — 클라이언트 / UI·아트 개발

#### 담당 역할
- Phaser 3 클라이언트 전체 설계 및 구현 — 화면 흐름(랜딩 → 로비 → 대기실 → 인게임),
  렌더링, 입력, 서버 연결 계층을 맡았다.
- 게임 화면 UI 전체(인게임 HUD, 코어 허브 창, 대기실, 가이드) 설계·구현.
- 픽셀아트 에셋 파이프라인 구축 — Aseprite Lua 생성기로 UI·이펙트 원본을 직접 만들고
  아틀라스로 굽는 자동화까지.
- 서버 스냅샷을 화면에 부드럽게 잇는 네트워크 보간·클라이언트 예측 구현.
- 플레이 피드백을 받아 "무엇이 안 보이는가 / 왜 안 눌리는가"를 UI 문제로 환원해
  반복적으로 개선.

#### 직접 구현한 영역
- **클라이언트 아키텍처**: 픽셀아트 선명도를 위한 네이티브 캔버스 + 정수배 카메라 줌
  전환, 월드/HUD 씬 분리, 로컬 모드(`LocalConnection`)와 멀티(`ColyseusConnection`)를
  같은 인터페이스(`GameConnection`)로 추상화 — 혼자하기와 멀티가 같은 화면 코드를 쓴다.
- **네트워크 보간·예측**: `SnapshotInterpolator`(패치 2주기 지연 보간, 지터 시 최대
  100ms 외삽, 장애물을 인지해 벽을 뚫지 않는 외삽), `PlayerPredictor`(입력 즉시 반영 후
  서버 좌표로 재조정), 서버 틱 상향분이 전달되지 않던 `patchRate` 미설정 버그 수정.
- **인게임 HUD**: 하단 퀵슬롯 바(체력·스태미나·경험치 게이지, 직업/스탯 칸), 코어 상태
  패널, 원형 시계(낮=시간 / 밤=남은 적 게이지), 미니맵(탐색 안개·건축 가능 영역·플레이어
  색 구분), 파티 체력 창, 보스 체력바, 쓰러짐 안내 배너.
- **코어 허브 창(탭 4종)**: 코어(게이지·강화·수리·충전·유령 부활) / 제작(티어·카테고리
  분류·스크롤) / 상점(진열·리롤) / 창고(격자·휴지통 드래그 폐기). 창고 격자와 하단
  퀵슬롯을 하나의 드래그 공간으로 묶는 공용 컨트롤러(`SlotDrag`) 설계.
- **픽셀 UI 에셋 제작**: Aseprite Lua 생성기 22종으로 게이지 9-slice 틀·채움, HUD
  아이콘(코어·자원·에너지·체력·검·휴지통 등), 키캡, 원형 시계, 타격·회복·레벨업 이펙트를
  직접 그렸다. `pnpm build:atlas`로 아틀라스를 굽는 빌드 파이프라인도 함께 구성.
- **전투·연출 렌더링**: 무기 좌우 반전·사선 자세·총구 정렬, 근접 사거리를 실제 그려진
  칼끝에 맞추는 보정, 피격 이펙트(흰 플래시·스파크), 시작 암전·DAY N·보스 경고·CLEAR
  연출, 낮–밤 하늘 전환과 코어를 중심으로 한 밤 광원.
- **로비·대기실**: 방 목록·방 만들기·코드 참가 모달, 대기실을 "강하 준비 브리핑실"로
  재설계(사원증 카드형 직업 선택, 지급품 퀵슬롯 표기, 채팅, 티모시 토글).
- **가이드 창**: 키캡 픽셀아트로 그린 조작표와 게임 진행 안내를 여러 쪽으로 나눠 담고,
  첫 입장(1일차)에 한 번 저절로 열리게 해 조작을 모른 채 시작하는 일이 없게 했다.
- **라이선스 에셋 분리 및 배포**: 재배포가 금지된 상용 몬스터 팩이 공개 저장소에 들어가지
  않도록 별도 비공개 저장소로 분리하고, 배포 빌드에서만 주입하도록 CI를 구성(Fine-grained
  PAT). GitHub Pages 클라이언트 배포를 담당.

#### 담당 결과 및 기여
서버가 20Hz로 보내는 상태를 60fps 화면으로 잇는 보간·예측 계층을 만들어, 네트워크 지연이
있어도 캐릭터가 끊기거나 되감기지 않는 조작감을 확보했다. 흩어져 있던 창고·제작·상점·강화
창을 코어 앞 탭 하나로 합치고 네 탭의 버튼 재질과 값 단위(자원·에너지 아이콘)를 한 어휘로
통일해, "무엇을 어디서 하는가"를 찾는 데 드는 시간을 줄였다. UI 작업은 대부분 실제 플레이
제보에서 출발했고("글자가 안 보인다", "클릭이 안 먹는다"), 원인을 픽셀 폰트 배율·입력 차단
범위 같은 구체적 이유로 좁혀 고친 뒤 화면으로 다시 확인했다.

#### 확인 가능 자료
- GitHub: `packages/client/`(클라이언트 전체), `packages/client/src/game/ui/`(HUD·모달),
  `assets/_generators/`(픽셀 에셋 Lua 생성기 22종)
- 작업 보고서: `docs/frontend/01~21번` — 화면 정책·에셋 파이프라인·작업별 기획과 결과.
- 커밋: 저장소 전체 커밋 중 약 140건. 무엇을 왜 바꿨는지 한국어로 서술했다.
- 배포: GitHub Pages 자동 배포(`.github/workflows/deploy-pages.yml`)


## 3. 협업 및 분업 방식

### 3.1 역할 분담 방식

팀은 게임 기획, 클라이언트 개발, AI·백엔드 개발, UI·에셋 및 배포 영역을 중심으로 역할을 분담했습니다.

각 팀원은 담당 기능을 구현하되, 게임 규칙과 기능 간 인터페이스는 팀 논의를 통해 사전에 합의했습니다.
특히 AI 동료의 행동 결과가 게임 화면과 자원 시스템에 정상적으로 반영되도록,
AI·백엔드 담당자와 클라이언트 담당자가 데이터 형식 및 처리 흐름을 함께 조율했습니다.


### 3.2 협업 절차

1. 게임의 핵심 목표, 코어 방어 규칙, 자원 활용 방식, AI 동료 역할을 함께 논의했습니다.
2. 기능을 담당 영역별로 분리하고, 각 팀원이 구현을 진행했습니다.
3. 구현 중 인터페이스 또는 게임 규칙 변경이 필요한 경우 [Discord / Notion / GitHub Issues / 회의 등 실제 도구]를 통해 공유했습니다.
4. 기능 구현 후 통합 환경에서 게임 플레이 테스트를 진행했습니다.
5. 발견된 오류와 개선 사항을 공유하고 수정한 뒤 재검증했습니다.


### 3.3 형상 관리 및 기여 확인

- 전체 소스 코드는 GitHub 저장소에서 관리했습니다.
- [Git Flow / feature 브랜치 / main 브랜치 직접 반영 등 실제 방식]으로 작업했습니다.
- 팀원별 구현 과정은 GitHub 커밋 기록을 통해 확인할 수 있습니다.
- 제출 시점까지 GitHub 저장소와 플레이 링크를 유지합니다.

GitHub Repository: https://github.com/CKCdefense/DropFall
